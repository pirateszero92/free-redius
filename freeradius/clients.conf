# FreeRADIUS clients.conf
# All NAS clients are managed via the Web GUI and stored in PostgreSQL (nas table)
# They are loaded dynamically via the SQL module (read_clients = yes)
#
# Note: localhost client is seeded in init.sql with secret 'testing123'
# You can add/edit NAS clients through the Web UI at http://localhost:8080

